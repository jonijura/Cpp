
#include <stdio.h>
#include <iostream>
int main(int argc, char** argv) {

    // Print the message
    printf("Hello World!");
    // Finalize the MPI environment.
    enum color { red, yellow, green, blue };

    std::cout << (green >> 1);


}